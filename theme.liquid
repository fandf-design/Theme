<!doctype html>
<html lang="{{ request.locale.iso_code }}" data-theme="{{ settings.default_theme | default: \"dark\" }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{{ page_title }}{% if shop.name %} – {{ shop.name }}{% endif %}</title>
  {{ content_for_header }}
  {{ "theme.css" | asset_url | stylesheet_tag }}
  {{ "theme.js" | asset_url | script_tag }}
</head>
<body>
{% section "header" %}
<main id="MainContent">
  {{ content_for_layout }}
</main>
{% section "footer" %}
</body>
</html>
